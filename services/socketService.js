class SocketService {
  constructor() {
    this.socket = null;
    this.isConnected = false;
  }

  // ... existing connection methods ...

  // Remove all code related to voice messages, voice recording, audioBlob, and any emit/on logic for voice_message, voice_recording, or sendVoiceMessage.
}

export default new SocketService(); 